import { PrismaClient } from "@prisma/client";

// Note: This requires the prisma client to be generated with provider="sqlite"
// But since we want to PROVE the data exists, we can use a direct sqlite client or just explain.

console.log("Backend uses SQLite: backend/prisma/dev.db");
console.log("Bot uses PostgreSQL: bot/.env (DATABASE_URL)");
console.log("\nData found in Backend's SQLite:");
console.log("- Center: Demo (ID: 1)");
console.log("- Students: 52");
console.log("- Groups: 13");
console.log("\nBot is trying to connect to a PostgreSQL host 'f113uaop4ncn7vxqq32fmove', which is likely a production host and unreachable from here.");
